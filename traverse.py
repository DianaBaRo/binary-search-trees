"""
We can do it 
    in order : left subtree + root node + right subtree recursively. It sorts the tree.
    pre order : root + left subtree + right subtree recursively.
    post order : left + right + root recursively
"""